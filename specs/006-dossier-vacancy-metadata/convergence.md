# Convergence: Dossier Vacancy Metadata

The deterministic parser, fictional archive regression, complete backend suite, and one
revision-checked local-vault repair converge on the specified result. The historical Two Spice
application now projects the explicit role and company from its packet, and the current
employer-linked job URL; its `preparing` stage and existing `hold` review are intact.

This change does not re-evaluate eligibility, send an application, or rewrite other imports.
The local repair is private vault state and is not committed. The code and specs remain
uncommitted because the configured SSH signing key is locked; do not bypass signed-commit
or protected-branch requirements to merge them.
